export default function UndefinedName({ username }: { username: string }) {
    return (
        <div className="w-full h-fit text-end text-gray-900 text-2xl font-bold font-inter">
            {username}
        </div>
    );
}
